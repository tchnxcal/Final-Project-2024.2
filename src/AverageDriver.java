// Driver class to test the Average class functionality
public class AverageDriver {
    public static void main(String[] args) {
        // Create an instance of the Average class which handles input, sorting, and mean calculation
        Average avg = new Average();

        // Output the results (sorted scores and mean) using the toString method
        System.out.println(avg);
    }
}
